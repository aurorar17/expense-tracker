class User {
  constructor(uid, fname, lname, email, password) {
    this.uid = uid;
    this.fname = fname;
    this.lname = lname;
    this.email = email;
    this.password = password;
  }
}

export default User;
